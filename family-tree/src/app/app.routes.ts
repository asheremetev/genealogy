import { Routes } from '@angular/router';
import { FamilyTreeComponent } from './components/family-tree/family-tree.component';

export const routes: Routes = [{ path: '', component: FamilyTreeComponent }];
